# [项目名称] · 设计系统

## 1. 设计语言

[用 3–5 句话描述产品的视觉定位、情感调性、目标用户感受。]

示例：
- 情绪可视化：把抽象心情变成可感知的视觉元素。
- 自然治愈：运用有机曲线与柔和色彩，营造放松氛围。
- 年轻活力：大圆角、明亮色、轻量动效，符合年轻用户审美。

## 2. 色彩系统

### 品牌色

| Token | 色值 | 用途 |
|-------|------|------|
| `--brand-primary` | `#000000` | 主按钮、强调文字 |
| `--brand-secondary` | `#000000` | 次级按钮、标签 |
| `--brand-light` | `#000000` | 背景装饰 |

### 功能色

| Token | 色值 | 用途 |
|-------|------|------|
| `--success` | `#000000` | 成功、完成 |
| `--warning` | `#000000` | 警告、提醒 |
| `--error` | `#000000` | 错误、失败 |
| `--info` | `#000000` | 信息提示 |

### 中性色

| Token | 色值 | 用途 |
|-------|------|------|
| `--text-primary` | `#000000` | 主要文字 |
| `--text-secondary` | `#000000` | 次要文字 |
| `--text-tertiary` | `#000000` | 辅助说明 |
| `--bg-page` | `#FFFFFF` | 页面背景 |
| `--bg-card` | `#FFFFFF` | 卡片背景 |
| `--line` | `#000000` | 分割线、边框 |

## 3. 字体系统

- **中文**：[填写，例如 PingFang SC, Microsoft YaHei]
- **英文/数字**：[填写，例如 SF Pro Display, Inter]
- **字重**：Regular (400), Medium (500), Semibold (600), Bold (700)

### 字号规范

| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| H1 大标题 | 28–32 px | 700 | 首页大标题 |
| H2 页面标题 | 22–24 px | 700 | 页面级标题 |
| H3 卡片标题 | 18–20 px | 600 | 卡片标题 |
| Body 正文 | 15–16 px | 400 | 正文 |
| Caption 说明 | 12–13 px | 400 | 标签、时间 |
| Micro 微字 | 10–11 px | 500 | 徽章、状态 |

## 4. 间距系统

基础单位为 [4 / 8] px：

| Token | 数值 | 用途 |
|-------|------|------|
| `--space-xs` | 4 px | 图标与文字间距 |
| `--space-sm` | 8 px | 行内间距 |
| `--space-md` | 16 px | 卡片内边距 |
| `--space-lg` | 24 px | 模块间距 |
| `--space-xl` | 32 px | 页面边距 |
| `--space-xxl` | 48 px | 大模块间距 |

## 5. 圆角与阴影

### 圆角

| Token | 数值 | 用途 |
|-------|------|------|
| `--radius-sm` | 8 px | 小按钮、标签 |
| `--radius-md` | 16 px | 卡片、输入框 |
| `--radius-lg` | 24 px | 大卡片 |
| `--radius-xl` | 32 px | 底部导航、浮层 |
| `--radius-full` | 9999 px | 头像、圆形按钮 |

### 阴影

| Token | 参数 | 用途 |
|-------|------|------|
| `--shadow-sm` | `0 2px 8px rgba(0,0,0,0.06)` | 轻微浮起 |
| `--shadow-md` | `0 8px 24px rgba(0,0,0,0.10)` | 卡片悬浮 |
| `--shadow-lg` | `0 16px 40px rgba(0,0,0,0.14)` | 弹窗、模态 |

## 6. 图标规范

- 风格：[线性 / 填充 / 双色 / 其他]
- 描边：[1.5 / 2] px
- 尺寸：24 × 24 px（Tab/导航），20 × 20 px（行内），16 × 16 px（辅助）
- 默认色：[填写]，选中色：[填写]

## 7. 组件规范

### 按钮

| 类型 | 背景 | 文字 | 圆角 | 高度 |
|------|------|------|------|------|
| Primary | `--brand-primary` | 白色 | 16 px | 48 px |
| Secondary | `--bg-card` | `--brand-primary` | 16 px | 44 px |
| Ghost | 透明 | `--brand-primary` | 12 px | 40 px |

### 卡片

- 背景：`--bg-card`
- 圆角：16–24 px
- 内边距：16–20 px
- 阴影：`--shadow-sm` 或 `--shadow-md`

### 输入框

- 高度：48 px
- 圆角：12–16 px
- 边框：1 px solid `--line`
- 聚焦边框：`--brand-primary`

### 导航 / Tab Bar

- 位置：[顶部 / 底部]
- 高度：[填写]
- 选中样式：[填写]
- 未选中样式：[填写]

## 8. 布局网格

- 目标设备：[填写，例如 iPhone 15 Pro / Desktop / iPad]
- 尺寸：[填写，例如 393 × 852 px]
- 内容边距：[填写]
- 列数：[单列 / 双列 / 12 列网格]

## 9. 插画 / 视觉风格

[描述插画风格：扁平、3D、手绘、几何、渐变、拟物等。]

## 10. 动效原则

- 入场动画：[例如 错落淡入 + 上滑，300 ms]
- 点击反馈：[例如 0.96 缩放，100 ms]
- 页面转场：[例如 底部上滑 / 淡入淡出]
